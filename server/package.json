{
  "name": "server",
  "version": "0.0.1",
  "description": "Server for accessing IPFS nodes on Kubernetes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.2"
  }
}
